## AiMod 移植进度（2026-07-24 03:59）

### 任务
把 standalone/父仓的瞄准栈+追踪+Web+配置+准星全搬到 E:\obs-heji\AiMod
计划文件：C:\Users\Administrator\.workbuddy\plans\quantum-aurora-turing.md（14步）

### 已完成（15/23 项）
1. ✅ 目录结构：src/aim/ src/core/ src/host/ src/tracking/ src/web/ webui/
2. ✅ 瞄准栈 31 文件复制到 src/aim/（FullAimBridge+8后端+AdaptivePID+4预测器+AbstractMouseController框架+MouseControllerFactory）
3. ✅ OBS shim 复制（obs-module.h, plugin-support.h, obs_stubs.cpp）
4. ✅ TrackerEngine 复制到 src/tracking/
5. ✅ CrosshairDetector 复制到 src/host/
6. ✅ ConfigStore 复制到 src/core/（强耦合 EngineLoop，需简化版 AimConfig.h 替代）
7. ✅ WebServer 复制到 src/web/（强耦合 EngineLoop，需简化版 SimpleWebServer 替代）
8. ✅ BmpUtil 复制到 src/host/
9. ✅ nlohmann/json 复制到 third_party/
10. ✅ 新写 AimConfig.h（简化配置结构）
11. ✅ 新写 DetAdapter.h（AiMod DetectionObject → ya::Detection 适配器）
12. ✅ 新写 SimpleWebServer.h/.cpp（Winsock2，不依赖 EngineLoop）
13. ✅ WebUI 4文件复制（index.html, app.js, obs_help.js, app.css）
14. ✅ AiMod.cpp include 区已改（加移植头文件）
15. ✅ AiMod.cpp 成员变量已改（FullAimBridge/TrackerEngine/ConfigStore/WebServer/CrosshairDetector）

### 未完成（8 项，约 50%）
- ❌ Init() 还在引用旧成员（m_tracker/m_motionSim/m_pidX/m_pidY/m_mouseCurve）
- ❌ DetectionLoop 旧瞄准流水线未替换为 FullAimBridge.tick()
- ❌ DisplayLoop 未改
- ❌ Release() 未改
- ❌ Run()/Stop() 未加 WebServer 生命周期
- ❌ 旧 include 未清理（KalmanFilter.h/curve.h/MotionSimulator.h）
- ❌ vcxproj 未更新（新文件+去掉 pid_x64.lib+加 setupapi.lib/hid.lib）
- ❌ main 函数未改（Config 加载/WebServer 启动/Crosshair 初始化）

### 用户决策记录
- 保留 AiMod 三线程（不合并）
- 保留 MotionSimulator + KalmanFilter.h（卡尔曼预测保留，关联由 TrackerEngine 接管）
- 只保留 AdaptivePID 一种算法
- 要 CrosshairDetector + WebUI + WebServer + ConfigStore
- 不要 FloatingPreview / 坐标导出 / CUDA/TRT EP
- 简化模型管理（路径可配置）
- 弃用 pid_x64.lib，清 cout/硬编码/自旋负债
